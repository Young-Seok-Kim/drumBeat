<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="NumberTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">80dp</item> <!-- weight와 같이 쓰려면 0dp -->
        <item name="android:autoSizeTextType">uniform</item>
        <item name="android:autoSizeMinTextSize">8sp</item>
        <item name="android:autoSizeMaxTextSize">36sp</item>
        <item name="android:autoSizeStepGranularity">2sp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@android:color/darker_gray</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:minHeight">80dp</item>
        <!-- 필요시 minHeight, maxHeight 추가 가능 -->
    </style>

    <style name="NumberImageView">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">80dp</item>   <!-- layout_weight 쓰는 경우 0dp 필수 -->
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginTop">@dimen/number_image_margin_top</item>
        <item name="android:adjustViewBounds">true</item>
        <item name="android:scaleType">fitCenter</item>
    </style>

    <style name="RemainingTimeTextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:gravity">center</item>
    </style>
</resources>

